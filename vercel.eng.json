{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/eng"
      }
    }
  ],
  "buildCommand": "npm run build:eng",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
