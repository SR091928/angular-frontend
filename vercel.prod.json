{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/prod"
      }
    }
  ],
  "buildCommand": "npm run build:prod",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
